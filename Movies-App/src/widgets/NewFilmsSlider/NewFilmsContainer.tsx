// import { newFilmsAPI } from "@/services/PostService";

// const NewFilmsContainer = () => {
//   const { data: films, error, isLoading } = newFilmsAPI.useGetNewFilmsQuery();

//   return (
//     <div className=" bg-primaryWhite">
//       {isLoading && <h1>Loading in progress</h1>}
//       {error && <h1>Error has occured</h1>}
//       {films &&
//         films.map((film) => {
//           console.log("Film", film);
//         })}
//     </div>
//   );
// };

// export default NewFilmsContainer;
