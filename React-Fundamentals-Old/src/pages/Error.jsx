import React from "react"

const Error = (props) => {
  return (
    <div>
      <h1 style={{color: 'red'}}>
        You entered non-existing page!
      </h1>
    </div>
  )
};

export default Error;
